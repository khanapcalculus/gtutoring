services:
  # Backend API
  - type: web
    name: gtutoring-backend
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production

  # Frontend Web Service
  - type: web
    name: gtutoring-frontend
    runtime: node
    rootDir: client
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: gtutoring-backend
          property: host
